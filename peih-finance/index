<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <title>Peih Finance</title>
  <style>
    :root {
      --bg-main: #050505;
      --bg-card: #121212;
      --bg-nav: rgba(20, 20, 20, 0.85);
      --text-main: #ffffff;
      --text-muted: #666666;
      --border-color: #2a2a2a;
      --accent-bg: #ffffff;
      --accent-text: #000000;
      --nav-active: #ffffff;
      --shadow-nav: rgba(0, 0, 0, 0.8); 
      --modal-bg: #121212;
      --form-bg: #1e1e1e;
      --input-text: #ffffff;
      --queue-item-bg: #2a2a2a;
      --btn-outline: #333;
      --tree-line: #444444;
      --bg-sub: #0a0a0a;
    }

    [data-theme="light"] {
      --bg-main: #f2f2f7;
      --bg-card: #ffffff;
      --bg-nav: rgba(255, 255, 255, 0.85);
      --text-main: #000000;
      --text-muted: #8e8e93;
      --border-color: #e5e5ea;
      --accent-bg: #000000;
      --accent-text: #ffffff;
      --nav-active: #000000;
      --shadow-nav: rgba(0, 0, 0, 0.1);
      --modal-bg: #f2f2f7; 
      --form-bg: #ffffff;  
      --input-text: #000000;
      --queue-item-bg: #ffffff;
      --bg-sub: #ebebeb;
      --btn-outline: #ccc;
      --tree-line: #cccccc;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
      -webkit-tap-highlight-color: transparent;
    }

    body {
      background-color: var(--bg-main);
      color: var(--text-main);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      transition: background-color 0.3s, color 0.3s;
    }

    #loginScreen, #mainApp {
      width: 100%;
      height: 100vh;
      display: flex;
      flex-direction: column;
    }

    #loginScreen {
      justify-content: center;
      align-items: center;
      padding: 20px;
    }

    .login-container {
      width: 100%;
      max-width: 400px;
      background-color: var(--bg-card);
      border: 1px solid var(--border-color);
      border-radius: 20px;
      padding: 40px 20px;
      text-align: center;
      transition: all 0.3s;
    }

    .login-title {
      font-size: 28px;
      font-style: italic;
      font-weight: 800;
      letter-spacing: 1px;
      margin-bottom: 5px;
    }

    .login-subtitle {
      font-size: 10px;
      color: var(--text-muted);
      letter-spacing: 2px;
      margin-bottom: 40px;
      text-transform: uppercase;
    }

    .input-group {
      margin-bottom: 20px;
      text-align: left;
    }

    .input-field {
      width: 100%;
      background-color: var(--bg-main);
      border: 1px solid var(--border-color);
      color: var(--text-main);
      padding: 16px;
      border-radius: 12px;
      font-size: 16px;
      outline: none;
      transition: all 0.3s;
    }

    .btn-submit {
      width: 100%;
      background-color: var(--accent-bg);
      color: var(--accent-text);
      border: none;
      padding: 16px;
      border-radius: 12px;
      font-size: 14px;
      font-weight: bold;
      cursor: pointer;
      margin-top: 10px;
      transition: all 0.3s;
    }

    #mainApp {
      display: none;
      position: relative;
      flex-grow: 1;
    }

    .tab-content {
      display: none;
      padding: 40px 20px 140px 20px;
      overflow-y: auto;
      height: 100vh;
    }

    .tab-content.active {
      display: block;
    }

    .dashboard-header {
      text-align: center;
      margin-bottom: 40px;
    }

    .header-label {
      font-size: 12px;
      color: var(--text-muted);
      letter-spacing: 1px;
      text-transform: uppercase;
      margin-bottom: 8px;
    }

    .total-amount {
      font-size: 42px;
      font-weight: bold;
      margin-bottom: 8px;
    }

    .converted-amount {
      font-size: 16px;
      color: var(--text-muted);
    }

    .section-title {
      font-size: 12px;
      color: var(--text-muted);
      letter-spacing: 1px;
      text-transform: uppercase;
      margin-bottom: 15px;
      margin-top: 20px;
    }

    .card {
      background-color: var(--bg-card);
      border: 1px solid var(--border-color);
      border-radius: 20px;
      padding: 20px;
      margin-bottom: 15px;
      transition: all 0.3s;
    }

    .card-row {
      display: flex;
      justify-content: space-between;
      align-items: flex-end;
      margin-bottom: 15px;
    }

    .card-value {
      font-size: 24px;
      font-weight: bold;
    }

    .card-label {
      font-size: 11px;
      color: var(--text-muted);
      text-transform: uppercase;
      letter-spacing: 1px;
      margin-top: 4px;
    }

    .card-reward-label {
      font-size: 11px;
      color: #4da6ff;
      text-transform: uppercase;
      letter-spacing: 1px;
      text-align: right;
    }

    .card-reward-label.green {
      color: #4dffa6;
    }

    .card-reward-value {
      font-size: 14px;
      text-align: right;
      margin-top: 4px;
    }

    .progress-bar-bg {
      width: 100%;
      height: 6px;
      background-color: var(--border-color);
      border-radius: 3px;
      overflow: hidden;
    }

    .progress-bar-fill {
      height: 100%;
      background-color: var(--accent-bg);
      border-radius: 3px;
    }

    .fab-container {
      position: fixed;
      bottom: 95px;
      left: 50%;
      transform: translateX(-50%);
      z-index: 100;
    }

    .fab-btn {
      width: 56px;
      height: 56px;
      border-radius: 28px;
      background-color: var(--accent-bg);
      color: var(--accent-text);
      display: flex;
      justify-content: center;
      align-items: center;
      box-shadow: 0 8px 20px var(--shadow-nav);
      border: none;
      cursor: pointer;
      transition: transform 0.2s, background-color 0.3s, color 0.3s;
    }

    .fab-btn svg {
      width: 24px;
      height: 24px;
      stroke: var(--accent-text);
      stroke-width: 2.5;
    }

    .fab-btn:active {
      transform: scale(0.92);
    }

    .bottom-nav {
      position: fixed;
      bottom: 25px;
      left: 50%;
      transform: translateX(-50%);
      width: 92%;
      max-width: 450px;
      background-color: var(--bg-nav);
      backdrop-filter: blur(15px);
      -webkit-backdrop-filter: blur(15px);
      border: 1px solid var(--border-color);
      border-radius: 40px;
      display: flex;
      justify-content: space-around;
      align-items: center;
      padding: 12px 10px;
      z-index: 90;
      box-shadow: 0 10px 30px var(--shadow-nav);
      transition: all 0.3s;
    }

    .nav-item {
      display: flex;
      flex-direction: column;
      align-items: center;
      color: var(--text-muted);
      cursor: pointer;
      flex: 1;
      transition: color 0.3s;
    }

    .nav-item.active {
      color: var(--nav-active);
    }

    .nav-icon {
      margin-bottom: 4px;
      display: flex;
      justify-content: center;
    }

    .nav-icon svg {
      width: 22px;
      height: 22px;
      fill: none;
      stroke: currentColor;
      stroke-width: 2;
      stroke-linecap: round;
      stroke-linejoin: round;
      transition: stroke 0.3s;
    }

    .nav-text {
      font-size: 10px;
      font-weight: 600;
    }

    .settings-btn {
      width: 100%;
      background-color: transparent;
      color: var(--text-main);
      border: 1px solid var(--border-color);
      padding: 16px;
      border-radius: 12px;
      font-size: 14px;
      font-weight: bold;
      cursor: pointer;
      margin-top: 10px;
      transition: all 0.3s;
    }
    .icon-btn {
      width: 38px;
      height: 38px;
      border-radius: 50%;
      background-color: var(--form-bg);
      border: 1px solid var(--border-color);
      display: flex;
      justify-content: center;
      align-items: center;
      cursor: pointer;
      color: var(--text-main);
      padding: 0;
      transition: all 0.2s;
    }

    .icon-btn:active {
      transform: scale(0.9);
      opacity: 0.8;
    }

    .icon-btn svg {
      stroke: var(--text-main);
    }

    /* Modal Chính */
    .modal-overlay {
      position: fixed;
      top: 100%;
      left: 0;
      width: 100%;
      height: 100vh;
      background-color: var(--modal-bg);
      z-index: 999;
      transition: top 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
      display: flex;
      flex-direction: column;
    }

    .modal-overlay.open {
      top: 0;
    }

    /* Sub Modal */
    .sub-modal {
      position: fixed;
      top: 0;
      left: 100%;
      width: 100%;
      height: 100vh;
      background-color: var(--modal-bg);
      z-index: 1000;
      transition: left 0.3s ease;
      display: flex;
      flex-direction: column;
    }

    .sub-modal.open {
      left: 0;
    }

    .sub-modal-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 20px;
      font-size: 18px;
      font-weight: bold;
      border-bottom: 1px solid var(--border-color);
    }

    .btn-icon-circle {
      width: 36px;
      height: 36px;
      border-radius: 18px;
      background-color: var(--form-bg);
      display: flex;
      justify-content: center;
      align-items: center;
      border: none;
      color: var(--text-main);
      cursor: pointer;
    }

    .list-container {
      flex: 1;
      padding: 15px;
      overflow-y: auto;
    }

    /* Style Cây thư mục */
    .tree-group {
      background-color: var(--form-bg);
      border-radius: 16px;
      margin-bottom: 20px;
      overflow: hidden;
    }

    .tree-parent {
      display: flex;
      align-items: center;
      padding: 16px;
      font-weight: 600;
      border-bottom: 1px solid var(--border-color);
      cursor: pointer;
    }

    .tree-child {
      display: flex;
      align-items: center;
      padding: 16px 16px 16px 50px;
      position: relative;
      border-bottom: 1px solid var(--border-color);
      cursor: pointer;
      color: var(--text-main);
    }

    .tree-child:last-child {
      border-bottom: none;
    }

    .tree-child::before {
      content: '';
      position: absolute;
      left: 26px;
      top: 0;
      bottom: 50%;
      border-left: 2px solid var(--tree-line);
      border-bottom: 2px solid var(--tree-line);
      width: 12px;
      border-bottom-left-radius: 8px;
    }

    .tree-child::after {
      content: '';
      position: absolute;
      left: 26px;
      top: 50%;
      bottom: 0;
      border-left: 2px solid var(--tree-line);
    }

    .tree-child:last-child::after {
      display: none;
    }

    .color-icon {
      width: 24px;
      height: 24px;
      border-radius: 12px;
      margin-right: 12px;
      display: flex;
      justify-content: center;
      align-items: center;
      font-size: 12px;
    }

    .search-bar-container {
      padding: 15px;
      background-color: var(--modal-bg);
      border-top: 1px solid var(--border-color);
    }

    .search-input {
      width: 100%;
      background-color: var(--form-bg);
      border: none;
      padding: 12px 40px;
      border-radius: 20px;
      color: var(--text-main);
      font-size: 15px;
      outline: none;
      background-image: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="%23888" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"></circle><line x1="21" y1="21" x2="16.65" y2="16.65"></line></svg>');
      background-repeat: no-repeat;
      background-position: 12px center;
      background-size: 18px;
    }

    .modal-content {
      flex: 1;
      padding: 20px;
      display: flex;
      flex-direction: column;
      overflow-y: auto;
    }

    .modal-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 20px;
      margin-top: 10px;
    }

    .btn-cancel {
      background-color: var(--border-color);
      color: var(--text-main);
      border: none;
      padding: 8px 16px;
      border-radius: 20px;
      font-size: 14px;
      cursor: pointer;
    }

    .mode-toggle {
      background-color: var(--form-bg);
      border-radius: 20px;
      display: flex;
      padding: 4px;
    }

    .mode-btn {
      padding: 8px 16px;
      font-size: 12px;
      font-weight: 600;
      color: var(--text-muted);
      border-radius: 16px;
      cursor: pointer;
    }

    .mode-btn.active {
      background-color: var(--border-color);
      color: var(--text-main);
    }

    .modal-title {
      text-align: center;
      font-size: 20px;
      margin-bottom: 20px;
      color: var(--text-main);
    }

    .transaction-type-segment {
      display: flex;
      background-color: var(--form-bg);
      border-radius: 12px;
      padding: 4px;
      margin-bottom: 20px;
    }

    .type-btn {
      flex: 1;
      text-align: center;
      padding: 10px 0;
      font-size: 13px;
      color: var(--text-muted);
      border-radius: 10px;
      cursor: pointer;
    }

    .type-btn.active {
      background-color: var(--border-color);
      color: var(--text-main);
    }

    .form-container {
      background-color: var(--form-bg);
      border-radius: 16px;
      padding: 0 15px;
      margin-bottom: 15px;
    }

    .form-row {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 15px 0;
      border-bottom: 1px solid var(--border-color);
      cursor: pointer;
    }

    .form-row:last-child {
      border-bottom: none;
    }

    .row-left {
      display: flex;
      align-items: center;
      gap: 12px;
      font-size: 15px;
      width: 100%;
    }

    .minimal-icon {
      width: 20px;
      height: 20px;
      fill: none;
      stroke: var(--text-muted);
      stroke-width: 2;
      stroke-linecap: round;
      stroke-linejoin: round;
    }

    .circle-icon {
      width: 20px;
      height: 20px;
      border-radius: 10px;
      background-color: var(--text-muted);
      opacity: 0.5;
    }

    .arrow {
      color: var(--text-muted);
      font-size: 20px;
    }

    .amount-row {
      align-items: flex-start;
      cursor: default;
    }

    .currency-select {
      background-color: var(--border-color);
      color: var(--text-main);
      padding: 6px 10px;
      border-radius: 6px;
      font-size: 13px;
      border: none;
      outline: none;
      margin-top: 10px;
      font-weight: 600;
      appearance: none;
      text-align: center;
      cursor: pointer;
    }

    .amount-input-wrapper {
      flex: 1;
      margin-left: 15px;
      display: flex;
      flex-direction: column;
    }

    .amount-label {
      font-size: 11px;
      color: var(--text-muted);
      margin-bottom: 4px;
    }

    .amount-input {
      background: transparent;
      border: none;
      color: var(--input-text);
      font-size: 36px;
      padding: 0;
      margin: 0;
      width: 100%;
      outline: none;
    }
    
    .note-input {
      background: transparent;
      border: none;
      color: var(--input-text);
      font-size: 15px;
      width: 100%;
      outline: none;
    }
    
    .note-input::placeholder {
      color: var(--text-muted);
    }

    .date-row {
      justify-content: flex-start;
      cursor: default;
    }

    .date-row .row-left {
      width: auto;
    }

    .date-selector {
      flex: 1;
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-left: 10px;
    }

    .date-text {
      color: #007aff;
      font-size: 14px;
      white-space: nowrap; 
    }

    .date-text-wrapper {
      position: relative;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .hidden-date {
      position: absolute;
      opacity: 0;
      width: 100%;
      height: 100%;
      top: 0;
      left: 0;
      cursor: pointer;
    }

    .date-nav {
      background: var(--border-color);
      border: none;
      color: var(--text-main);
      width: 24px;
      height: 24px;
      border-radius: 12px;
      cursor: pointer;
    }

    .modal-bottom {
      display: flex;
      gap: 10px;
      margin-top: auto;
      padding-top: 20px;
    }
    
    .btn-queue {
      flex: 1;
      background-color: transparent;
      color: var(--text-main);
      border: 1px solid var(--btn-outline);
      padding: 16px;
      border-radius: 16px;
      font-size: 14px;
      font-weight: 600;
      cursor: pointer;
    }

    .btn-save-batch {
      flex: 1;
      background-color: #007aff;
      color: #fff;
      border: none;
      padding: 16px;
      border-radius: 16px;
      font-size: 16px;
      font-weight: bold;
      cursor: pointer;
    }

    /* Queue UI Styles */
    .queue-container {
      margin-top: 15px;
      display: flex;
      flex-direction: column;
      gap: 10px;
    }

    .queue-item {
      background-color: var(--queue-item-bg);
      border-radius: 12px;
      padding: 12px 15px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .queue-details {
      display: flex;
      flex-direction: column;
    }

    .queue-cat {
      font-size: 14px;
      font-weight: 600;
      color: var(--text-main);
    }

    .queue-note {
      font-size: 12px;
      color: var(--text-muted);
      margin-top: 2px;
    }

    .queue-amount-wrapper {
      display: flex;
      align-items: center;
      gap: 15px;
    }

    .queue-amount {
      font-size: 16px;
      font-weight: bold;
      color: var(--text-main);
    }
    
    .color-expense { color: #ff3b30 !important; } 
    .color-income { color: #34c759 !important; } 

    .btn-remove-queue {
      background: none;
      border: none;
      color: var(--text-muted);
      font-size: 20px;
      cursor: pointer;
      padding: 5px;
    }
    .ai-checkbox {
      width: 24px;
      height: 24px;
      border-radius: 12px;
      border: 2px solid var(--text-muted);
      display: flex;
      justify-content: center;
      align-items: center;
      cursor: pointer;
      transition: all 0.2s;
    }
    .ai-checkbox.checked {
      background-color: #007aff;
      border-color: #007aff;
    }
    .ai-checkbox.checked::after {
      content: '✓';
      color: white;
      font-size: 14px;
      font-weight: bold;
    }
    
    .ai-card {
      background-color: var(--form-bg);
      border-radius: 16px;
      margin-bottom: 12px;
      overflow: hidden;
      border: 1px solid var(--border-color);
    }
    .ai-card-header {
      display: flex;
      align-items: center;
      padding: 15px;
      cursor: pointer;
    }
    .ai-card-body {
      display: none;
      padding: 0 15px;
      border-top: 1px solid var(--border-color);
      background-color: var(--bg-sub);
    }
    .ai-card-body.open {
      display: block;
      animation: slideDown 0.3s ease-out forwards;
    }
    .ai-arrow {
      margin-left: 10px;
      color: var(--text-muted);
      transition: transform 0.3s;
      display: flex;
      align-items: center;
    }
    .ai-arrow svg {
      width: 20px;
      height: 20px;
      stroke: currentColor;
      stroke-width: 2;
      fill: none;
    }
    .ai-arrow.open {
      transform: rotate(180deg);
    }
    .ai-input-sleek {
      background: transparent;
      border: none;
      color: var(--text-main);
      font-size: 15px;
      width: 100%;
      outline: none;
      text-align: right;
    }
    @keyframes slideDown {
      from { opacity: 0; transform: translateY(-10px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .ledger-header { 
      display: flex; justify-content: space-between; align-items: center; margin-bottom: 25px; position: relative; min-height: 40px;
    }
    .wallet-selector { 
      position: absolute; left: 50%; transform: translateX(-50%);
      display: flex; flex-direction: column; align-items: center; cursor: pointer; 
      background: transparent; padding: 0; border: none; width: auto;
    }
    .wallet-name { 
      background: var(--form-bg); border: 1px solid var(--border-color); 
      padding: 6px 16px; border-radius: 20px;
      font-size: 14px; font-weight: 600; display: flex; align-items: center; justify-content: center;
      position: relative; padding-right: 30px; min-width: 130px;
    }
    .wallet-name svg { position: absolute; right: 12px; top: 50%; transform: translateY(-50%); color: var(--text-muted); }
    .wallet-balance { font-size: 13px; font-weight: bold; color: var(--text-main); margin-top: 6px; }
    .right-actions { display: flex; gap: 10px; }
    .time-tabs-container { 
      border-bottom: 1px solid var(--border-color); margin-bottom: 20px; 
      overflow-x: auto; white-space: nowrap; scrollbar-width: none; 
    }
    .time-tabs-container::-webkit-scrollbar { display: none; }
    .time-tabs { display: flex; gap: 20px; padding-bottom: 12px; justify-content: center; }
    .time-tab { 
      font-size: 12px; color: var(--text-muted); font-weight: 600; cursor: pointer; 
      text-transform: uppercase; letter-spacing: 0.5px; position: relative; transition: color 0.3s;
    }
    .time-tab.active { color: var(--text-main); }
    .time-tab.active::after { 
      content: ''; position: absolute; bottom: -13px; left: 0; width: 100%; height: 2px; background: var(--text-main); 
    }
    .ledger-summary { margin-bottom: 25px; font-size: 14px; padding: 0 10px; }
    .summary-row { display: flex; justify-content: space-between; padding: 4px 0; }
    .summary-divider { height: 1px; background: var(--border-color); margin: 8px 0; width: 50%; margin-left: auto; }
    .summary-link { text-align: center; color: #34c759; font-size: 12px; margin-top: 15px; cursor: pointer; }
    .btn-back-today { 
      position: fixed; 
      bottom: 105px;
      left: calc(50% + 38px); 
      transform: none;
      background: var(--text-main); color: var(--bg-main);
      border: none; padding: 8px 16px; border-radius: 20px; 
      font-weight: bold; font-size: 12px;
      box-shadow: 0 4px 15px rgba(0,0,0,0.3); cursor: pointer; z-index: 95; display: none;
    }
    [data-theme="light"] .btn-back-today { box-shadow: 0 4px 15px rgba(0,0,0,0.2); }
    .bottom-sheet-backdrop {
      position: fixed; top: 0; left: 0; width: 100%; height: 100vh;
      background: rgba(0,0,0,0.6); z-index: 1000;
      opacity: 0; visibility: hidden; transition: all 0.3s;
    }
    .bottom-sheet-backdrop.open { opacity: 1; visibility: visible; }
    
    .bottom-sheet-pane {
      position: absolute; bottom: 0; left: 0; width: 100%;
      background: var(--bg-main); border-radius: 20px 20px 0 0;
      transform: translateY(100%); transition: transform 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
      max-height: 90vh; display: flex; flex-direction: column;
      padding-bottom: constant(safe-area-inset-bottom);
      padding-bottom: env(safe-area-inset-bottom);
    }
    .bottom-sheet-backdrop.open .bottom-sheet-pane { transform: translateY(0); }
    
    .sheet-header {
      display: flex; justify-content: space-between; align-items: center;
      padding: 15px 20px;
    }
    .sheet-title { font-size: 16px; font-weight: 700; color: var(--text-main); }
    .sheet-btn { 
      background: var(--form-bg); border: 1px solid var(--border-color); 
      color: var(--text-main); font-size: 13px; font-weight: 600; 
      padding: 6px 14px; border-radius: 20px; cursor: pointer;
    }
    .sheet-list { padding: 10px 20px 30px 20px; overflow-y: auto; }
    .popover-backdrop {
      position: fixed; top: 0; left: 0; width: 100%; height: 100vh;
      z-index: 1000; display: none; /* Dùng backdrop trong suốt để chặn click ra ngoài */
    }
    .popover-backdrop.open { display: block; }
    
    .popover-menu {
      position: absolute; top: 80px; right: 20px; width: 220px;
      background: rgba(30, 30, 30, 0.95); backdrop-filter: blur(15px); -webkit-backdrop-filter: blur(15px);
      border: 1px solid var(--border-color); border-radius: 16px;
      box-shadow: 0 10px 40px rgba(0,0,0,0.5); overflow: hidden;
      transform: scale(0.9); opacity: 0; transition: all 0.2s cubic-bezier(0.25, 0.8, 0.25, 1); 
      pointer-events: none;
    }
    [data-theme="light"] .popover-menu {
      background: rgba(255, 255, 255, 0.95); box-shadow: 0 10px 40px rgba(0,0,0,0.1);
    }
    .popover-backdrop.open .popover-menu {
      transform: scale(1); opacity: 1; pointer-events: auto;
    }
    .popover-item {
      padding: 14px 20px; font-size: 15px; color: var(--text-main); font-weight: 500;
      border-bottom: 1px solid var(--border-color); cursor: pointer; text-align: center;
    }
    .popover-item:last-child { border-bottom: none; }
    .popover-item:active { background: rgba(255, 255, 255, 0.1); }
    [data-theme="light"] .popover-item:active { background: rgba(0, 0, 0, 0.05); }
  </style>
</head>
<body>

  <div id="loginScreen">
    <div class="login-container">
      <div class="login-title">PEIH FINANCE</div>
      <div class="login-subtitle">AUTHENTICATION REQUIRED</div>
      <div class="input-group">
        <input type="email" id="emailInput" class="input-field" placeholder="Email sếp">
      </div>
      <div class="input-group">
        <input type="password" id="passwordInput" class="input-field" placeholder="Mật khẩu">
      </div>
      <button class="btn-submit" onclick="handleLogin()">XÁC NHẬN VÀO HỆ THỐNG</button>
    </div>
  </div>

  <div id="mainApp">
    
    <div id="tab-dashboard" class="tab-content active">
      <div class="dashboard-header">
        <div class="header-label">TỔNG CHI TIÊU</div>
        <div class="total-amount">¥ 1.184.858</div>
        <div class="converted-amount">≈ 195.501.570 VND</div>
      </div>
      <div class="section-title">KỶ LUẬT & PHÁT TRIỂN</div>
      <div class="card">
        <div class="card-row">
          <div>
            <div class="card-value">12 / 50</div>
            <div class="card-label">KẾ HOẠCH CHẠY BỘ</div>
          </div>
          <div>
            <div class="card-reward-label">PHẦN THƯỞNG</div>
            <div class="card-reward-value">Nạp Suica ¥5000</div>
          </div>
        </div>
        <div class="progress-bar-bg">
          <div class="progress-bar-fill" style="width: 24%;"></div>
        </div>
      </div>
    </div>

    <div id="tab-ledger" class="tab-content">
      
      <div class="ledger-header">
        <button class="icon-btn">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"></circle><path d="M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3"></path><line x1="12" y1="17" x2="12.01" y2="17"></line></svg>
        </button>
        
        <div class="wallet-selector" onclick="openLedgerWalletMenu()">
          <div class="wallet-name">
            <span id="ledgerWalletName">🌐 Tổng cộng</span> 
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="6 9 12 15 18 9"></polyline></svg>
          </div>
          <div class="wallet-balance" id="ledgerWalletBalance">¥ 11,202,340</div>
        </div>
        
        <div class="right-actions">
          <button class="icon-btn">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"></circle><line x1="21" y1="21" x2="16.65" y2="16.65"></line></svg>
          </button>
          <button class="icon-btn" onclick="openLedgerMainMenu()">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="1.5"></circle><circle cx="19" cy="12" r="1.5"></circle><circle cx="5" cy="12" r="1.5"></circle></svg>
          </button>
        </div>
      </div>

      <div class="time-tabs-container">
        <div class="time-tabs" id="ledgerTimeTabs">
          <div class="time-tab">01/04/2026</div>
          <div class="time-tab">HÔM QUA</div>
          <div class="time-tab active">HÔM NAY</div>
          <div class="time-tab">TƯƠNG LAI</div>
        </div>
      </div>

      <div class="card ledger-summary" style="padding: 15px 20px;">
        <div class="summary-row">
          <span style="color: var(--text-muted);">Tiền vào</span>
          <span class="" id="ledgerIncomeTotal">¥ 0</span>
        </div>
        <div class="summary-row">
          <span style="color: var(--text-muted);">Tiền ra</span>
          <span class="" id="ledgerExpenseTotal">-¥ 89,888</span>
        </div>
        <div class="summary-divider"></div>
        <div class="summary-row" style="justify-content: flex-end">
          <span id="ledgerGrandTotal" style="font-weight: bold;">-¥ 89,888</span>
        </div>
        <div class="summary-link">Xem báo cáo cho giai đoạn này</div>
      </div>
      <div id="ledgerGroupToggle" class="transaction-type-segment" style="display: none; margin-bottom: 15px;">
        <div class="type-btn active" onclick="setLedgerGroupMode('category-parent')" id="btnGroupParent">Nhóm Cha</div>
        <div class="type-btn" onclick="setLedgerGroupMode('category-child')" id="btnGroupChild">Nhóm Con</div>
      </div>
      <div id="ledgerTxContainer" style="display: flex; flex-direction: column; gap: 10px;">
        </div>

      <button id="btnBackToToday" class="btn-back-today" onclick="backToToday()">Trở về hiện tại</button>
      
    </div>
    <div id="tab-pantry" class="tab-content">...</div>
    <div id="tab-budget" class="tab-content">...</div>

    <div id="tab-settings" class="tab-content">
      <div class="dashboard-header">
        <div class="header-label">HỆ THỐNG</div>
        <div class="total-amount">Cài đặt</div>
      </div>
      <div class="card">
        <button class="settings-btn" onclick="toggleTheme()">ĐỔI GIAO DIỆN SÁNG / TỐI</button>
        <button class="settings-btn" onclick="logout()" style="color: #ff4d4d; border-color: #ff4d4d;">ĐĂNG XUẤT</button>
      </div>
    </div>

    <div class="fab-container">
      <button class="fab-btn" onclick="openAddModal()">
        <svg viewBox="0 0 24 24"><line x1="12" y1="5" x2="12" y2="19"></line><line x1="5" y1="12" x2="19" y2="12"></line></svg>
      </button>
    </div>

    <div class="bottom-nav">
      <div class="nav-item active" onclick="switchTab('dashboard', this)">
        <div class="nav-icon"><svg viewBox="0 0 24 24"><rect x="3" y="3" width="7" height="9"></rect><rect x="14" y="3" width="7" height="5"></rect><rect x="14" y="12" width="7" height="9"></rect><rect x="3" y="16" width="7" height="5"></rect></svg></div>
        <div class="nav-text">Tổng quan</div>
      </div>
      <div class="nav-item" onclick="switchTab('ledger', this)">
        <div class="nav-icon"><svg viewBox="0 0 24 24"><line x1="8" y1="6" x2="21" y2="6"></line><line x1="8" y1="12" x2="21" y2="12"></line><line x1="8" y1="18" x2="21" y2="18"></line><line x1="3" y1="6" x2="3.01" y2="6"></line><line x1="3" y1="12" x2="3.01" y2="12"></line><line x1="3" y1="18" x2="3.01" y2="18"></line></svg></div>
        <div class="nav-text">Sổ sách</div>
      </div>
      <div class="nav-item" onclick="switchTab('pantry', this)">
        <div class="nav-icon"><svg viewBox="0 0 24 24"><path d="M6 2L3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"></path><line x1="3" y1="6" x2="21" y2="6"></line><path d="M16 10a4 4 0 0 1-8 0"></path></svg></div>
        <div class="nav-text">Đi chợ</div>
      </div>
      <div class="nav-item" onclick="switchTab('budget', this)">
        <div class="nav-icon"><svg viewBox="0 0 24 24"><rect x="1" y="4" width="22" height="16" rx="2" ry="2"></rect><line x1="1" y1="10" x2="23" y2="10"></line></svg></div>
        <div class="nav-text">Ngân sách</div>
      </div>
      <div class="nav-item" onclick="switchTab('settings', this)">
        <div class="nav-icon"><svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="3"></circle><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1 0 2.83 2 2 0 0 1-2.83 0l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-2 2 2 2 0 0 1-2-2v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83 0 2 2 0 0 1 0-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1-2-2 2 2 0 0 1 2-2h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 0-2.83 2 2 0 0 1 2.83 0l.06.06a1.65 1.65 0 0 0 1.82.33H9a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 2-2 2 2 0 0 1 2 2v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 0 2 2 0 0 1 0 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 2 2 2 2 0 0 1-2 2h-.09a1.65 1.65 0 0 0-1.51 1z"></path></svg></div>
        <div class="nav-text">Cài đặt</div>
      </div>
    </div>
  </div>

  <div id="addTransactionModal" class="modal-overlay">
    <div class="modal-content">
      
      <div class="modal-header">
        <button class="btn-cancel" onclick="closeAddModal()">Huỷ</button>
        <div class="mode-toggle">
          <div class="mode-btn active" id="btnModeNumpad" onclick="switchInputMode('manual')">THỦ CÔNG</div>
          <div class="mode-btn" id="btnModeAi" onclick="switchInputMode('ai')">PHÂN TÍCH ✨</div>
        </div>
        <div style="width: 60px;"></div>
      </div>

      <h2 class="modal-title">Thêm Giao Dịch</h2>

      <div id="manualInputSection" style="display: flex; flex-direction: column; flex: 1;">
        <div class="transaction-type-segment">
          <div class="type-btn active" onclick="changeMainType('Khoản chi')">Khoản chi</div>
          <div class="type-btn" onclick="changeMainType('Khoản thu')">Khoản thu</div>
          <div class="type-btn" onclick="changeMainType('Vay/Nợ')">Vay/Nợ</div>
        </div>

        <div class="form-container">
          
          <div class="form-row" onclick="currentAiEditIndex = null; openWalletModal()">
            <div class="row-left">
              <svg class="minimal-icon" viewBox="0 0 24 24"><path d="M21 12V7H5a2 2 0 0 1 0-4h14v2h2V3a2 2 0 0 0-2-2H5a4 4 0 0 0 0 8h16v13a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-7h2v7h14v-7z"></path><path d="M16 15a2 2 0 1 0 0-4 2 2 0 0 0 0 4z"></path></svg>
              <span id="mainWalletText" style="margin-left: 12px;">Đang tải...</span>
              <input type="hidden" id="hiddenWallet" value="">
            </div>
            <span class="arrow">›</span>
          </div>
          
          <div class="form-row amount-row">
            <select class="currency-select">
              <option value="VND">VND</option>
              <option value="USD">USD</option>
              <option value="JPY">JPY</option>
            </select>
            <div class="amount-input-wrapper">
              <span class="amount-label">Số tiền</span>
              <input type="number" class="amount-input" value="" placeholder="0" inputmode="decimal">
            </div>
          </div>

          <div class="form-row" onclick="currentAiEditIndex = null; openCategoryModal()">
            <div class="row-left">
            <div class="circle-icon" id="mainCategoryIcon"></div>
            <span id="mainCategoryText" style="margin-left: 12px; color: var(--text-muted);">Chọn nhóm</span>
            <input type="hidden" id="hiddenCategory" value="">
          </div> 
            <span class="arrow">›</span>
          </div>

          <div class="form-row" id="manualPersonRow" style="display: none;" onclick="currentAiEditIndex = null; openPersonModal()">
            <div class="row-left">
              <svg style="width:20px; height:20px; stroke:#ff3b30; fill:none;" viewBox="0 0 24 24" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"></path><line x1="12" y1="9" x2="12" y2="13"></line><line x1="12" y1="17" x2="12.01" y2="17"></line></svg>
              <span id="mainPersonText" style="margin-left: 12px; color: #ff3b30; font-weight: bold;">Chọn người vay/ cho vay</span>
              <input type="hidden" id="hiddenPerson" value="">
            </div>
            <span class="arrow">›</span>
          </div>

          <div class="form-row">
            <div class="row-left">
              <svg class="minimal-icon" viewBox="0 0 24 24"><path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"></path><path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"></path></svg>
              <input type="text" class="note-input" style="margin-left:12px;" placeholder="Ghi chú">
            </div>
          </div>

          <div class="form-row date-row">
            <div class="row-left">
              <svg class="minimal-icon" viewBox="0 0 24 24"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect><line x1="16" y1="2" x2="16" y2="6"></line><line x1="8" y1="2" x2="8" y2="6"></line><line x1="3" y1="10" x2="21" y2="10"></line></svg>
            </div>
            <div class="date-selector">
              <button class="date-nav" onclick="changeDate(-1)">‹</button>
              <div class="date-text-wrapper">
                <span id="displayDate" class="date-text"></span>
                <input type="date" id="nativeDateInput" class="hidden-date" onchange="syncDateFromNative()">
              </div>
              <button class="date-nav" onclick="changeDate(1)">›</button>
            </div>
          </div>

        </div>

        <div id="queueListContainer" class="queue-container"></div>

        <div class="modal-bottom">
          <button class="btn-queue" onclick="queueTransaction()">+ Thêm giao dịch</button>
          <button class="btn-save-batch" onclick="submitBatch()">Lưu <span id="queueCount" style="display:none;">(0)</span></button>
        </div>
      </div>
      <div id="aiInputSection" style="display: none; flex-direction: column; flex: 1;">
        <div class="form-container" style="padding: 15px;">
          <textarea id="aiPromptInput" placeholder="VD: Sáng ăn phở 40k, trưa đổ xăng 50k bằng thẻ Suica..." style="width: 100%; height: 100px; background: transparent; border: none; color: var(--input-text); font-size: 15px; outline: none; resize: none;"></textarea>
          
          <div style="display: flex; justify-content: space-between; align-items: center; margin-top: 10px; border-top: 1px solid var(--border-color); padding-top: 10px;">
            <select id="aiCurrencySelect" class="currency-select" style="margin-top: 0;" onchange="syncAiCurrencies(this.value)">
              <option value="VND">VND</option>
              <option value="JPY">JPY</option>
              <option value="USD">USD</option>
            </select>
            <button id="btnAiParse" onclick="parseAIPrompt()" style="background: #007aff; color: #fff; border: none; padding: 10px 20px; border-radius: 12px; font-weight: bold; cursor: pointer;">Phân tích ✨</button>
          </div>
        </div>

        <div id="aiParsedListContainer" class="queue-container" style="flex: 1; overflow-y: auto;"></div>

        <div class="modal-bottom" style="margin-top: auto; padding-top: 20px;">
          <button id="btnAiAddAll" class="btn-save-batch" onclick="addAiTransactions('all')">Thêm tất cả</button>
          <button id="btnAiAddSelected" class="btn-save-batch" style="display:none; background-color: #28a745;" onclick="addAiTransactions('selected')">Thêm mục đã chọn</button>
        </div>
      </div>
    </div>
  </div>

  <div id="categoryModal" class="sub-modal">
    <div class="sub-modal-header">
      <button class="btn-icon-circle" onclick="closeCategoryModal()">‹</button>
      <span>Chọn nhóm</span>
      <button class="btn-icon-circle">
        <svg viewBox="0 0 24 24" width="20" height="20" stroke="currentColor" stroke-width="2" fill="none"><line x1="3" y1="12" x2="21" y2="12"></line><line x1="3" y1="6" x2="21" y2="6"></line><line x1="3" y1="18" x2="21" y2="18"></line></svg>
      </button>
    </div>
    
    <div style="padding: 15px 15px 0 15px;">
      <div class="transaction-type-segment" style="margin-bottom: 0;">
        <div class="type-btn cat-tab-btn active" onclick="switchCategoryTab('Khoản chi')">Khoản chi</div>
        <div class="type-btn cat-tab-btn" onclick="switchCategoryTab('Khoản thu')">Khoản thu</div>
        <div class="type-btn cat-tab-btn" onclick="switchCategoryTab('Vay/Nợ')">Vay/Nợ</div>
      </div>
    </div>

    <div id="categoryTreeContainer" class="list-container">
      </div>

    <div class="search-bar-container">
      <input type="text" class="search-input" placeholder="Tìm kiếm nhóm...">
    </div>
  </div>

  <div id="walletModal" class="sub-modal">
    <div class="sub-modal-header">
      <button class="btn-icon-circle" onclick="closeWalletModal()">‹</button>
      <span>Chọn ví</span>
      <button class="btn-icon-circle">+</button>
    </div>
    <div id="walletListContainer" class="list-container">
      <div class="tree-group" id="walletListGroup">
        </div>
    </div>
  </div>
  <div id="personModal" class="sub-modal">
    <div class="sub-modal-header">
      <button class="btn-icon-circle" onclick="closePersonModal()">‹</button>
      <span>Chọn người Vay / Nợ</span>
      <button class="btn-icon-circle" onclick="addNewPerson()">+</button>
    </div>
    <div id="personListContainer" class="list-container">
      </div>
  </div>
  <div id="ledgerWalletSheet" class="bottom-sheet-backdrop" onclick="closeLedgerWalletMenu(event)">
    <div class="bottom-sheet-pane" onclick="event.stopPropagation()">
      <div class="sheet-header">
        <button class="sheet-btn" onclick="closeLedgerWalletMenu('force')">Đóng</button>
        <div class="sheet-title">Chọn Ví</div>
        <button class="sheet-btn">Sửa</button>
      </div>
      
      <div class="sheet-list">
        <div class="tree-group">
           <div class="tree-parent" onclick="toggleLedgerWallet('all')">
              <div class="color-icon" style="background:#007aff; border:none; color:white; width: 28px; height: 28px; font-size:14px;">🌐</div>
              <div style="flex:1;">
                 <div style="font-weight: 600; font-size: 15px;">Tổng cộng</div>
                 <div style="font-size:12px; color:var(--text-muted); margin-top:2px;">Mọi giao dịch</div>
              </div>
              <div id="check-wallet-all" style="color: #007aff; font-weight: bold; font-size: 16px; display: block;">✓</div>
           </div>
        </div>
        
        <div class="section-title">TÍNH VÀO TỔNG</div>
        
        <div class="tree-group" id="ledgerWalletListGroup">
           </div>
      </div>
    </div>
  </div>
  <div id="ledgerMainMenu" class="popover-backdrop" onclick="closeLedgerMainMenu()">
    <div class="popover-menu" onclick="event.stopPropagation()">
      <div class="popover-item" onclick="openTimePeriodMenu()">Khoảng thời gian</div>
      <div class="popover-item" id="menuGroupToggle" onclick="toggleLedgerMainGroupMode()">Xem theo nhóm</div>
      <div class="popover-item" onclick="alert('Tính năng Chuyển tiền đang phát triển')">Chuyển tiền đến ví khác</div>
      <div class="popover-item" onclick="alert('Tính năng Điều chỉnh số dư đang phát triển')">Điều chỉnh số dư</div>
      <div class="popover-item" onclick="alert('Tính năng Đồng bộ đang phát triển')">Đồng bộ ví</div>
    </div>
  </div>

  <div id="ledgerTimeMenu" class="popover-backdrop" onclick="closeTimePeriodMenu()">
    <div class="popover-menu" onclick="event.stopPropagation()">
      <div class="popover-item" onclick="selectLedgerTimeMode('Ngày')">Ngày</div>
      <div class="popover-item" onclick="selectLedgerTimeMode('Tuần')">Tuần</div>
      <div class="popover-item" onclick="selectLedgerTimeMode('Tháng')">Tháng</div>
      <div class="popover-item" onclick="selectLedgerTimeMode('Quý')">Quý</div>
      <div class="popover-item" onclick="selectLedgerTimeMode('Năm')">Năm</div>
      <div class="popover-item" onclick="selectLedgerTimeMode('Tất cả')">Tất cả</div>
      <div class="popover-item" onclick="alert('Tuỳ chỉnh ngày đang phát triển')">Tuỳ chỉnh</div>
    </div>
  </div>
  <script>
    // --- APP LOGIC ---
    function handleLogin() {
      const email = document.getElementById('emailInput').value;
      const pass = document.getElementById('passwordInput').value;
      const loginBtn = document.querySelector('.btn-submit');
      loginBtn.innerText = "ĐANG KIỂM TRA...";
      setTimeout(() => {
        if(email !== "" && pass !== "") {
          document.getElementById('loginScreen').style.display = 'none';
          document.getElementById('mainApp').style.display = 'flex';
          loadAppConfig(); 
        } else {
          alert("Sếp vui lòng nhập đủ Email và Mật khẩu!");
          loginBtn.innerText = "XÁC NHẬN VÀO HỆ THỐNG";
        }
      }, 800);
    }

    function switchTab(tabId, element) {
      document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('active'));
      document.querySelectorAll('.nav-item').forEach(i => i.classList.remove('active'));
      document.getElementById('tab-' + tabId).classList.add('active');
      element.classList.add('active');
      if (tabId === 'ledger') renderTimeTabs();
    }

    function toggleTheme() {
      const body = document.body;
      if (body.getAttribute('data-theme') === 'light') {
        body.removeAttribute('data-theme');
      } else {
        body.setAttribute('data-theme', 'light');
      }
    }

    function logout() {
      document.getElementById('loginScreen').style.display = 'flex';
      document.getElementById('mainApp').style.display = 'none';
      switchTab('dashboard', document.querySelectorAll('.nav-item')[0]);
    }

    // --- MODAL & DATE LOGIC ---
    let currentDate = new Date();

    function updateDateDisplay() {
      const days = ["Chủ Nhật", "Thứ Hai", "Thứ Ba", "Thứ Tư", "Thứ Năm", "Thứ Sáu", "Thứ Bảy"];
      const dayName = days[currentDate.getDay()];
      const dd = String(currentDate.getDate()).padStart(2, '0');
      const mm = String(currentDate.getMonth() + 1).padStart(2, '0');
      const yyyy = currentDate.getFullYear();
      document.getElementById('displayDate').innerText = `${dayName}, ${dd}/${mm}/${yyyy}`;
      document.getElementById('nativeDateInput').value = `${yyyy}-${mm}-${dd}`;
    }

    function changeDate(offset) {
      currentDate.setDate(currentDate.getDate() + offset);
      updateDateDisplay();
    }

    function syncDateFromNative() {
      const inputVal = document.getElementById('nativeDateInput').value;
      if(inputVal) {
        currentDate = new Date(inputVal);
        updateDateDisplay();
      }
    }
    // --- LOGIC CHỌN VÍ SỔ SÁCH (MULTI-SELECT) ---
    let selectedLedgerWallets = ['all']; 

    function openLedgerWalletMenu() {
      if (!appConfig) return alert("Sếp chưa đăng nhập hoặc dữ liệu chưa tải xong!");
      document.getElementById('ledgerWalletSheet').classList.add('open');
      renderLedgerWalletList();
    }

    function closeLedgerWalletMenu(eventOrAction) {
      if (eventOrAction !== 'force' && eventOrAction.target !== document.getElementById('ledgerWalletSheet')) return;
      document.getElementById('ledgerWalletSheet').classList.remove('open');
      updateLedgerTopBar();
      renderLedgerTransactions();
    }

    function renderLedgerWalletList() {
      const container = document.getElementById('ledgerWalletListGroup');
      container.innerHTML = '';
      const isAll = selectedLedgerWallets.includes('all');
      document.getElementById('check-wallet-all').style.display = isAll ? 'block' : 'none';

      appConfig.wallets.forEach(wallet => {
        const wName = wallet.name || wallet;
        const wIcon = wallet.icon || "💳";
        const isSelected = selectedLedgerWallets.includes(wName);

        const row = document.createElement('div');
        row.className = 'tree-parent';
        row.style.borderBottom = "1px solid var(--border-color)";
        row.innerHTML = `
          <div class="color-icon" style="background-color: var(--form-bg); border: 1px solid var(--border-color); width: 28px; height: 28px;">${wIcon}</div>
          <div style="flex: 1; font-weight: 500; font-size: 15px;">${wName}</div>
          <div style="color: #007aff; font-weight: bold; font-size: 16px; display: ${isSelected ? 'block' : 'none'};">✓</div>
        `;
        row.onclick = () => toggleLedgerWallet(wName);
        container.appendChild(row);
      });
      if(container.lastChild) container.lastChild.style.borderBottom = "none";
    }

    function toggleLedgerWallet(wName) {
      if (wName === 'all') {
        selectedLedgerWallets = ['all']; 
      } else {
        if (selectedLedgerWallets.includes('all')) {
          selectedLedgerWallets = [];
        }
        
        const index = selectedLedgerWallets.indexOf(wName);
        if (index > -1) {
          selectedLedgerWallets.splice(index, 1); 
        } else {
          selectedLedgerWallets.push(wName); 
        }
        if (selectedLedgerWallets.length === 0) {
          selectedLedgerWallets = ['all'];
        }
      }
      renderLedgerWalletList();
    }
    function updateLedgerTopBar() {
       const topBarName = document.getElementById('ledgerWalletName');
       if (selectedLedgerWallets.includes('all')) {
          topBarName.innerHTML = '🌐 Tổng cộng';
       } else if (selectedLedgerWallets.length === 1) {
          topBarName.innerHTML = selectedLedgerWallets[0];
       } else {
          topBarName.innerHTML = `Đã chọn ${selectedLedgerWallets.length} ví`;
       }
    }
    function openAddModal() {
      currentAiEditIndex = null;
      document.getElementById('addTransactionModal').classList.add('open');
      updateDateDisplay();
      switchInputMode('manual'); 
      setTimeout(() => { document.querySelector('.amount-input').focus(); }, 400);
    }

    function closeAddModal() {
      document.getElementById('addTransactionModal').classList.remove('open');
    }

    // --- BACKEND & CONFIG LOGIC ---
    const GAS_WEB_APP_URL = "https://script.google.com/macros/s/AKfycbwMlOvI6Fbw4AqnAf-09WgFlviIH9XOecyP6LBzgILvPnIXcXpTkwWKUqHjrwYaBi2kuw/exec";

    let appConfig = null;
    let transactionQueue = [];
    let currentMainType = 'Khoản chi'; 
    let currentAiEditIndex = null;
    let allLedgerData = []; 

    async function loadLedgerData() {
      const userEmail = document.getElementById('emailInput').value; 
      try {
        const userEmail = document.getElementById('emailInput').value;
        const response = await fetch(GAS_WEB_APP_URL, {
        method: "POST",
        body: JSON.stringify({ action: "GET_TRANSACTIONS", email: userEmail }),
        headers: { "Content-Type": "text/plain;charset=utf-8" }
      });
        const data = await response.json();
        if (data.status === "success") {
          allLedgerData = data.data;
          renderLedgerTransactions(); 
        }
      } catch (err) {
        console.log("Lỗi tải Sổ sách:", err);
      }
    }
    async function loadAppConfig() {
      const userEmail = document.getElementById('emailInput').value;
      sessionStorage.clear();
      try {
        const response = await fetch(GAS_WEB_APP_URL, {
          method: "POST",
          body: JSON.stringify({ action: "GET_CONFIG", email: userEmail }),
          headers: { "Content-Type": "text/plain;charset=utf-8" }
        });
        const data = await response.json();
        if (data.status === "success") {
          appConfig = data.data;
          if (appConfig.wallets.length === 0) {
            alert("Không tìm thấy dữ liệu cho tài khoản: " + userEmail + ". Sếp kiểm tra lại cột Email trong Google Sheets xem đã gõ đúng chữ '" + userEmail + "' chưa nhé!");
          }  
          initDefaultValues();
          loadLedgerData();
        } else {
          alert("Lỗi từ máy chủ Google! Sếp nhớ Triển khai phiên bản mới nhé.");
        }
      } catch (err) { 
        alert("Lỗi đường truyền! Hãy kiểm tra lại link GAS.");
      }
    }

    function initDefaultValues() {
      if(!appConfig || !appConfig.wallets || appConfig.wallets.length === 0) return;
      const defaultWallet = appConfig.wallets[0].name ? appConfig.wallets[0].name : appConfig.wallets[0];
      document.getElementById('hiddenWallet').value = defaultWallet;
      document.getElementById('mainWalletText').innerText = defaultWallet;
    }

    function changeMainType(typeStr) {
      if (currentMainType !== typeStr) {
        document.getElementById('mainCategoryText').innerText = "Chọn nhóm";
        document.getElementById('mainCategoryText').style.color = "var(--text-muted)";
        document.getElementById('hiddenCategory').value = "";
        const catIconNode = document.getElementById('mainCategoryIcon');
        if (catIconNode) {
          catIconNode.className = "circle-icon";
          catIconNode.style.cssText = "";
          catIconNode.innerHTML = "";
        }
      }
      const personRow = document.getElementById('manualPersonRow');
      if (personRow) {
        personRow.style.display = (typeStr === 'Vay/Nợ') ? 'flex' : 'none';
        if (typeStr !== 'Vay/Nợ') {
           document.getElementById('mainPersonText').innerText = 'Chọn đối tác';
           document.getElementById('mainPersonText').style.color = "#ff3b30";
           document.getElementById('hiddenPerson').value = '';
        }
      }
      currentMainType = typeStr;
      
      const mainBtns = document.querySelectorAll('.transaction-type-segment .type-btn:not(.cat-tab-btn)');
      mainBtns.forEach(b => {
        if(b.innerText === typeStr) b.classList.add('active');
        else b.classList.remove('active');
      });
    }

    function openCategoryModal() {
      if(!appConfig) return;
      document.getElementById('categoryModal').classList.add('open');
      switchCategoryTab(currentMainType); 
    }

    function closeCategoryModal() {
      document.getElementById('categoryModal').classList.remove('open');
    }

    function closeWalletModal() {
      document.getElementById('walletModal').classList.remove('open');
    }

    function openWalletModal() {
      if(!appConfig) return;
      document.getElementById('walletModal').classList.add('open');
      
      const groupDiv = document.getElementById('walletListGroup');
      groupDiv.innerHTML = '';

      if (!appConfig.wallets || appConfig.wallets.length === 0) {
        groupDiv.innerHTML = '<div style="padding: 20px; text-align: center; color: var(--text-muted);">Sếp chưa cài đặt Ví nào trong Sheets!</div>';
        return;
      }

      appConfig.wallets.forEach(wallet => {
        const walletName = wallet.name ? wallet.name : wallet;
        const walletIcon = wallet.icon ? wallet.icon : "💳";

        const row = document.createElement('div');
        row.className = 'tree-parent'; 
        row.style.borderBottom = "1px solid var(--border-color)";
        row.innerHTML = `<div class="color-icon" style="background-color: var(--form-bg); border: 1px solid var(--border-color);">${walletIcon}</div><span>${walletName}</span>`;
        row.onclick = () => {
          if (currentAiEditIndex !== null) {
            aiParsedResults[currentAiEditIndex].wallet = walletName;
            renderAiList(currentAiEditIndex); // Giữ nguyên thẻ sửa
          } else {
            document.getElementById('mainWalletText').innerText = walletName;
            document.getElementById('hiddenWallet').value = walletName;
          }
          closeWalletModal();
        };
        groupDiv.appendChild(row);
      });
      if(groupDiv.lastChild) groupDiv.lastChild.style.borderBottom = "none";
    }

    function switchCategoryTab(typeStr) {
      const tabBtns = document.querySelectorAll('.cat-tab-btn');
      tabBtns.forEach(b => {
        if(b.innerText === typeStr) b.classList.add('active');
        else b.classList.remove('active');
      });

      const container = document.getElementById('categoryTreeContainer');
      container.innerHTML = '';
      
      if (!appConfig || !appConfig.categories || !appConfig.categories[typeStr]) return;
      const typeData = appConfig.categories[typeStr];
      
      if (Object.keys(typeData).length === 0) {
        container.innerHTML = '<div style="padding: 20px; text-align: center; color: var(--text-muted);">Sếp chưa cài đặt Nhóm nào cho loại này!</div>';
        return;
      }

      const colors = ['#f1c40f', '#1abc9c', '#e74c3c', '#3498db', '#9b59b6'];
      let colorIndex = 0;

      for (const parent in typeData) {
        const bgIconColor = colors[colorIndex % colors.length];
        colorIndex++;

        const groupDiv = document.createElement('div');
        groupDiv.className = 'tree-group';

        const parentRow = document.createElement('div');
        parentRow.className = 'tree-parent';
        parentRow.innerHTML = `<div class="color-icon" style="background-color: ${bgIconColor}; color: #000;">${parent.charAt(0)}</div><span>${parent}</span>`;
        groupDiv.appendChild(parentRow);

        typeData[parent].forEach(child => {
          const childName = child.name ? child.name : child;
          const childIcon = child.icon ? child.icon : "✨";

          const childRow = document.createElement('div');
          childRow.className = 'tree-child';
          
          childRow.innerHTML = `<div class="color-icon" style="background-color: var(--form-bg); border: 1px solid var(--border-color);">${childIcon}</div><span>${childName}</span>`;
          
          childRow.onclick = () => {
            const fullCatName = parent + " - " + childName;
            
            if (currentAiEditIndex !== null) {
              aiParsedResults[currentAiEditIndex].category = fullCatName;
              aiParsedResults[currentAiEditIndex].type = typeStr;
              renderAiList(currentAiEditIndex);
            } else {
              changeMainType(typeStr);
              document.getElementById('mainCategoryText').innerText = childName;
              document.getElementById('mainCategoryText').style.color = "var(--text-main)";
              document.getElementById('hiddenCategory').value = fullCatName;
              const catIconNode = document.getElementById('mainCategoryIcon');
              if (catIconNode) {
                catIconNode.className = "color-icon";
                catIconNode.style.backgroundColor = "var(--form-bg)";
                catIconNode.style.border = "1px solid var(--border-color)";
                catIconNode.style.width = "20px";
                catIconNode.style.height = "20px";
                catIconNode.style.marginRight = "0"; 
                catIconNode.style.fontSize = "11px"; 
                
                catIconNode.innerHTML = childIcon;
              }
            }
            closeCategoryModal();
          };
          groupDiv.appendChild(childRow);
        });
        container.appendChild(groupDiv);
      }
    }

    function renderQueue() {
      const container = document.getElementById('queueListContainer');
      container.innerHTML = '';
      
      transactionQueue.forEach((tx, index) => {
        let isIncome = false;
        if (tx.type === "Khoản thu") {
          isIncome = true;
        } else if (tx.type === "Vay/Nợ" && tx.category.includes("Đi vay")) {
          isIncome = true; 
        }
        
        const sign = isIncome ? "+" : "-";
        const colorClass = isIncome ? "color-income" : "color-expense";
        
        const itemHtml = `
          <div class="queue-item">
            <div class="queue-details">
              <span class="queue-cat">${tx.category}</span>
              <span class="queue-note">${tx.note || tx.wallet}</span>
            </div>
            <div class="queue-amount-wrapper">
              <span class="queue-amount ${colorClass}">${sign}${parseInt(tx.amount).toLocaleString()} ${tx.currency}</span>
              <button class="btn-remove-queue" onclick="removeFromQueue(${index})">✕</button>
            </div>
          </div>
        `;
        container.insertAdjacentHTML('beforeend', itemHtml);
      });

      const countLabel = document.getElementById('queueCount');
      if (transactionQueue.length > 0) {
        countLabel.innerText = `(${transactionQueue.length})`;
        countLabel.style.display = 'inline';
      } else {
        countLabel.style.display = 'none';
      }
    }

    function removeFromQueue(index) {
      transactionQueue.splice(index, 1);
      renderQueue();
    }

    function queueTransaction() {
      const amount = document.querySelector('.amount-input').value;
      if (!amount || amount === "0") {
        alert("Sếp chưa nhập số tiền!");
        return;
      }
      const category = document.getElementById('hiddenCategory').value;
      if (!category) {
        alert("Sếp vui lòng chọn nhóm!");
        return;
      }

      const tx = {
        type: currentMainType,
        wallet: document.getElementById('hiddenWallet').value,
        currency: document.querySelector('.currency-select').value,
        amount: amount,
        category: category,
        person: document.getElementById('hiddenPerson') ? document.getElementById('hiddenPerson').value : "",
        note: document.querySelector('.note-input').value,
        date: document.getElementById('nativeDateInput').value
      };

      transactionQueue.push(tx);
      renderQueue();
      
      document.querySelector('.amount-input').value = "";
      document.querySelector('.note-input').value = "";
      document.getElementById('mainCategoryText').innerText = "Chọn nhóm";
      document.getElementById('mainCategoryText').style.color = "var(--text-muted)";
      document.getElementById('hiddenCategory').value = "";
      const catIconNode = document.getElementById('mainCategoryIcon');
      if (catIconNode) {
        catIconNode.className = "circle-icon";
        catIconNode.style.cssText = "";
        catIconNode.innerHTML = "";
      }
      document.querySelector('.amount-input').focus();
      if(document.getElementById('hiddenPerson')) document.getElementById('hiddenPerson').value = "";
    }

    function submitBatch() {
      if (transactionQueue.length === 0) {
        const currentAmount = document.querySelector('.amount-input').value;
        if (currentAmount && currentAmount !== "0") {
          queueTransaction();
        } else {
          alert("Không có giao dịch nào để lưu!");
          return;
        }
      }

      const btn = document.querySelector('.btn-save-batch');
      btn.innerText = "Đang lưu...";
      
      const userEmail = document.getElementById('emailInput').value; 

      fetch(GAS_WEB_APP_URL, {
        method: "POST",
        body: JSON.stringify({ action: "SAVE_BATCH", transactions: transactionQueue, email: userEmail }), 
        headers: { "Content-Type": "text/plain;charset=utf-8" }
      })
      .then(res => res.json())
      .then(data => {
        if(data.status === "success") {
          alert(`Đã lưu thành công ${transactionQueue.length} giao dịch!`);
          transactionQueue = [];
          renderQueue();
          closeAddModal();
          btn.innerText = "Lưu";
        }
      })
      .catch(() => {
        alert("Có lỗi kết nối mạng!");
        btn.innerText = "Lưu";
      });
    }
    let aiParsedResults = [];
    function getAiCatIcon(fullCatName, type) {
      if(!appConfig || !appConfig.categories[type]) return "📊";
      const childName = fullCatName.split(' - ').pop();
      for (const parent in appConfig.categories[type]) {
        const found = appConfig.categories[type][parent].find(c => c.name === childName);
        if (found) return found.icon;
      }
      return "📊";
    }
    function getAiWalletIcon(walletName) {
      if(!appConfig || !appConfig.wallets) return "💳";
      const found = appConfig.wallets.find(w => (w.name || w) === walletName);
      return found ? (found.icon || "💳") : "💳";
    }
    function syncAiCurrencies(newVal) {
      if (aiParsedResults.length > 0) {
        aiParsedResults.forEach(tx => {
          tx.currency = newVal;
        });
        renderAiList();
      }
    }

    function switchInputMode(mode) {
      document.getElementById('btnModeNumpad').classList.remove('active');
      document.getElementById('btnModeAi').classList.remove('active');
      document.getElementById('manualInputSection').style.display = 'none';
      document.getElementById('aiInputSection').style.display = 'none';

      if (mode === 'manual') {
        document.getElementById('btnModeNumpad').classList.add('active');
        document.getElementById('manualInputSection').style.display = 'flex';
      } else {
        document.getElementById('btnModeAi').classList.add('active');
        document.getElementById('aiInputSection').style.display = 'flex';
      }
    }

    async function parseAIPrompt() {
      const text = document.getElementById('aiPromptInput').value;
      if (!text) return alert("Sếp chưa nhập nội dung!");
      
      const btn = document.getElementById('btnAiParse');
      btn.innerText = "Đang xử lý...";
      btn.disabled = true;

      try {
        const response = await fetch(GAS_WEB_APP_URL, {
          method: "POST",
          body: JSON.stringify({ 
            action: "PARSE_AI", 
            text: text,
            currency: document.getElementById('aiCurrencySelect').value,
            config: appConfig 
          }),
          headers: { "Content-Type": "text/plain;charset=utf-8" }
        });
        const data = await response.json();
        
        if (data.status === "success") {
          const selectedCurrency = document.getElementById('aiCurrencySelect').value;
          aiParsedResults = data.data.map(tx => ({ ...tx, currency: selectedCurrency })); 
          renderAiList();
        } else {
          alert(data.message || "Có lỗi khi phân tích!");
        }
      } catch(e) {
        alert("Lỗi đường truyền tới Peih Advisory!");
      }
      btn.innerText = "Phân tích ✨";
      btn.disabled = false;
    }

    function toggleAiEdit(index) {
      const body = document.getElementById(`ai-body-${index}`);
      const arrow = document.getElementById(`ai-arrow-${index}`);
      if (body.classList.contains('open')) {
        body.classList.remove('open');
        arrow.classList.remove('open');
      } else {
        body.classList.add('open');
        arrow.classList.add('open');
      }
    }

    function renderAiList(keepOpenIndex = null) {
      const container = document.getElementById('aiParsedListContainer');
      container.innerHTML = '';
      const currentCurrency = document.getElementById('aiCurrencySelect').value;
      
      aiParsedResults.forEach((tx, index) => {
        // Kiểm tra xem giao dịch này có phải Vay/Nợ không
        const isDebt = tx.type === "Vay/Nợ" || tx.category.includes("vay") || tx.category.includes("nợ");
        const isIncome = (tx.type === "Khoản thu" || (isDebt && (tx.category.includes("Đi vay") || tx.category.includes("Thu nợ"))));
        const sign = isIncome ? "+" : "-";
        const colorClass = isIncome ? "color-income" : "color-expense";
        
        const isOpenClass = (keepOpenIndex === index) ? "open" : "";

        // Icon tam giác cảnh báo tối giản (chỉ sinh ra nếu là Vay/Nợ)
        const warningIcon = isDebt ? `<svg style="width:18px; height:18px; stroke:#ff3b30; fill:none; margin-right:6px; vertical-align:text-bottom;" viewBox="0 0 24 24" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"></path><line x1="12" y1="9" x2="12" y2="13"></line><line x1="12" y1="17" x2="12.01" y2="17"></line></svg>` : '';

        const itemHtml = `
          <div class="ai-card">
            <div class="ai-card-header" onclick="toggleAiEdit(${index})">
              <div class="ai-checkbox checked" onclick="event.stopPropagation(); toggleAiCheck(this)"></div>
              <div style="flex: 1; margin-left: 15px;">
                <div style="font-weight: 700; font-size: 16px; color: var(--text-main); display: flex; align-items: center;">
                  ${warningIcon} ${tx.category.split(' - ').pop()}
                </div>
                <div style="font-size: 12px; color: var(--text-muted); margin-top: 2px;">${tx.wallet} • ${tx.note || "Ghi chú trống"}</div>
              </div>
              <div style="text-align: right;">
                <div class="queue-amount ${colorClass}" style="font-size: 17px;">${sign}${parseInt(tx.amount).toLocaleString()}</div>
                <div style="font-size: 11px; color: var(--text-muted); font-weight: 600;">${tx.currency || currentCurrency}</div>
              </div>
              <div id="ai-arrow-${index}" class="ai-arrow ${isOpenClass}">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"></polyline></svg>
              </div>
            </div>

            <div id="ai-body-${index}" class="ai-card-body ${isOpenClass}">
              
              ${isDebt ? `
              <div class="form-row" style="padding: 12px 0;" onclick="currentAiEditIndex=${index}; openPersonModal();">
                <div class="row-left" style="display: flex; align-items: center;">
                  ${warningIcon}
                  <span class="ai-label-mini" style="color: #ff3b30; width: auto; margin-right: 10px;">Đối tác</span>
                </div>
                <div class="ai-input-sleek" style="display:flex; align-items:center; justify-content:flex-end; gap:8px;">
                  <span style="color: ${tx.person ? 'var(--text-main)' : '#ff3b30'}; font-weight: bold;">${tx.person || "⚠️ Bấm chọn tên"}</span>
                  <span class="arrow" style="font-size:14px;">›</span>
                </div>
              </div>
              ` : ''}

              <div class="form-row" style="padding: 12px 0;" onclick="currentAiEditIndex=${index}; openCategoryModal();">
                <div class="row-left"><span class="ai-label-mini">Nhóm</span></div>
                <div class="ai-input-sleek" style="display:flex; align-items:center; justify-content:flex-end; gap:8px;">
                  <span>${tx.category}</span>
                  <span class="arrow" style="font-size:14px;">›</span>
                </div>
              </div>
              
              <div class="form-row" style="padding: 12px 0;" onclick="currentAiEditIndex=${index}; openWalletModal();">
                <div class="row-left"><span class="ai-label-mini">Ví</span></div>
                <div class="ai-input-sleek" style="display:flex; align-items:center; justify-content:flex-end; gap:8px;">
                  <span>${tx.wallet}</span>
                  <span class="arrow" style="font-size:14px;">›</span>
                </div>
              </div>

              <div class="form-row" style="padding: 12px 0; align-items: center;">
                <div class="row-left"><span class="ai-label-mini">Ghi chú</span></div>
                <textarea class="ai-input-sleek" rows="1" style="resize: none; text-align: right; font-family: inherit; overflow: hidden; line-height: 1.5; padding: 6px 10px;"
                          oninput="this.style.height = ''; this.style.height = this.scrollHeight + 'px'"
                          onchange="aiParsedResults[${index}].note = this.value">${tx.note || ""}</textarea>
              </div>

              <div class="form-row" style="border-bottom: none; padding: 12px 0;">
                <div class="row-left">
                  <span class="ai-label-mini">Số tiền</span>
                  <select class="currency-select" style="margin-top:0; padding: 2px 6px; font-size: 11px;" 
                          onchange="aiParsedResults[${index}].currency = this.value; renderAiList(${index});">
                    <option value="VND" ${tx.currency === 'VND' ? 'selected' : ''}>VND</option>
                    <option value="JPY" ${tx.currency === 'JPY' ? 'selected' : ''}>JPY</option>
                    <option value="USD" ${tx.currency === 'USD' ? 'selected' : ''}>USD</option>
                  </select>
                </div>
                <input type="number" class="ai-input-sleek ${colorClass}" style="font-size: 22px; font-weight: 800;" value="${tx.amount}" onchange="aiParsedResults[${index}].amount = this.value">
              </div>
            </div>
          </div>
        `;
        container.insertAdjacentHTML('beforeend', itemHtml);
      });
      updateAiButtons();
    }

    function toggleAiCheck(element) {
      element.classList.toggle('checked');
      updateAiButtons();
    }

    function updateAiButtons() {
      const checkedBoxes = document.querySelectorAll('.ai-checkbox.checked');
      const btnAll = document.getElementById('btnAiAddAll');
      const btnSelected = document.getElementById('btnAiAddSelected');
      
      if (checkedBoxes.length > 0 && checkedBoxes.length < aiParsedResults.length) {
        btnAll.style.display = 'none';
        btnSelected.style.display = 'block';
        btnSelected.innerText = `Thêm mục đã chọn (${checkedBoxes.length})`;
      } else if (checkedBoxes.length === 0) {
        btnAll.style.display = 'block';
        btnSelected.style.display = 'none';
        btnAll.innerText = "Không có mục nào được chọn";
        btnAll.disabled = true;
      } else {
        btnAll.style.display = 'block';
        btnSelected.style.display = 'none';
        btnAll.innerText = "Thêm tất cả";
        btnAll.disabled = false;
      }
    }

    function addAiTransactions(mode) {
      const date = document.getElementById('nativeDateInput').value;
      const checkedBoxes = document.querySelectorAll('.ai-checkbox');

      aiParsedResults.forEach((tx, index) => {
        if (mode === 'all' || (mode === 'selected' && checkedBoxes[index].classList.contains('checked'))) {
          transactionQueue.push({
            type: tx.type,
            wallet: tx.wallet,
            currency: tx.currency,
            amount: tx.amount,
            category: tx.category,
            person: tx.person || "",
            note: tx.note,
            date: date
          });
        }
      });
      aiParsedResults = [];
      document.getElementById('aiPromptInput').value = "";
      document.getElementById('aiParsedListContainer').innerHTML = "";
      
      renderQueue(); 
      switchInputMode('manual'); 
    }
    // --- LOGIC QUẢN LÝ ĐỐI TÁC (VAY/NỢ) ---
    function openPersonModal() {
      document.getElementById('personModal').classList.add('open');
      renderPersonList();
    }

    function closePersonModal() {
      document.getElementById('personModal').classList.remove('open');
    }

    function renderPersonList() {
      const container = document.getElementById('personListContainer');
      container.innerHTML = '';

      const people = (appConfig && appConfig.activeDebtors) ? appConfig.activeDebtors : [];

      if (people.length === 0) {
        container.innerHTML = '<div style="padding: 20px; text-align: center; color: var(--text-muted);">Sạch nợ! Không có ai đang vay/nợ sếp. Bấm dấu + góc trên để thêm thủ công.</div>';
      } else {
        people.forEach(person => {
          const row = document.createElement('div');
          row.className = 'tree-parent'; 
          row.style.borderBottom = "1px solid var(--border-color)";
          row.innerHTML = `<div class="color-icon" style="background-color: var(--form-bg); border: 1px solid var(--border-color);">👤</div><span>${person}</span>`;
          row.onclick = () => selectPerson(person);
          container.appendChild(row);
        });
      }
    }

    function selectPerson(personName) {
      if (currentAiEditIndex !== null) {
        aiParsedResults[currentAiEditIndex].person = personName;
        renderAiList(currentAiEditIndex);
      } else {
        document.getElementById('mainPersonText').innerText = personName;
        document.getElementById('mainPersonText').style.color = "var(--text-main)";
        document.getElementById('hiddenPerson').value = personName;
      }
      closePersonModal();
    }

    function addNewPerson() {
      const name = prompt("Nhập tên Đối tác Vay / Cho vay mới:");
      if (name && name.trim() !== "") {
        if(!appConfig.activeDebtors) appConfig.activeDebtors = [];
        if(!appConfig.activeDebtors.includes(name.trim())) {
          appConfig.activeDebtors.push(name.trim());
        }
        selectPerson(name.trim());
      }
    }
    function openLedgerMainMenu() {
      document.getElementById('ledgerMainMenu').classList.add('open');
    }

    function closeLedgerMainMenu() {
      document.getElementById('ledgerMainMenu').classList.remove('open');
    }

    function openTimePeriodMenu() {
      closeLedgerMainMenu(); 
      setTimeout(() => {
        document.getElementById('ledgerTimeMenu').classList.add('open'); 
      }, 100);
    }

    function closeTimePeriodMenu() {
      document.getElementById('ledgerTimeMenu').classList.remove('open');
    }

    let currentLedgerTimeMode = 'Tháng'; 
    let currentLedgerTimeOffset = 0; 

    function getLedgerTimeRange(mode, offset) {
      let start = new Date();
      let end = new Date();
      let label = "";
      let today = new Date();
      start.setHours(0,0,0,0);
      end.setHours(23,59,59,999);

      if (mode === 'Ngày') {
        start.setDate(today.getDate() + offset);
        end.setDate(today.getDate() + offset);
        if (offset === 0) label = "HÔM NAY";
        else if (offset === -1) label = "HÔM QUA";
        else if (offset === 1) label = "NGÀY MAI";
        else label = start.toLocaleDateString('vi-VN', { day: '2-digit', month: '2-digit', year: 'numeric' });
      } 
      else if (mode === 'Tuần') {
        let day = today.getDay() || 7; 
        start.setDate(today.getDate() - day + 1 + (offset * 7));
        end.setDate(start.getDate() + 6);
        if (offset === 0) label = "TUẦN NÀY";
        else if (offset === -1) label = "TUẦN TRƯỚC";
        else if (offset === 1) label = "TUẦN SAU";
        else {
           // Ép hiển thị chuẩn 13/04 - 19/04
           let sDay = String(start.getDate()).padStart(2, '0');
           let sMon = String(start.getMonth() + 1).padStart(2, '0');
           let eDay = String(end.getDate()).padStart(2, '0');
           let eMon = String(end.getMonth() + 1).padStart(2, '0');
           label = `${sDay}/${sMon} - ${eDay}/${eMon}`;
        }
      }
      else if (mode === 'Tháng') {
        start.setMonth(today.getMonth() + offset, 1);
        end.setMonth(start.getMonth() + 1, 0); 
        if (offset === 0) label = "THÁNG NÀY";
        else if (offset === -1) label = "THÁNG TRƯỚC";
        else if (offset === 1) label = "THÁNG SAU";
        else label = "THÁNG " + (start.getMonth() + 1) + "/" + start.getFullYear();
      }
      else if (mode === 'Quý') {
        let currentQuarter = Math.floor(today.getMonth() / 3);
        start.setMonth((currentQuarter + offset) * 3, 1);
        end.setMonth(start.getMonth() + 3, 0);
        if (offset === 0) label = "QUÝ NÀY";
        else if (offset === -1) label = "QUÝ TRƯỚC";
        else if (offset === 1) label = "QUÝ SAU";
        else label = "QUÝ " + (Math.floor(start.getMonth()/3) + 1) + "/" + start.getFullYear();
      }
      else if (mode === 'Năm') {
        start.setFullYear(today.getFullYear() + offset, 0, 1);
        end.setFullYear(today.getFullYear() + offset, 11, 31);
        if (offset === 0) label = "NĂM NAY";
        else if (offset === -1) label = "NĂM NGOÁI";
        else if (offset === 1) label = "NĂM SAU";
        else label = "NĂM " + start.getFullYear();
      }
      else {
        label = "TẤT CẢ THỜI GIAN";
      }
      
      return { start, end, label };
    }

    function selectLedgerTimeMode(mode) {
      currentLedgerTimeMode = mode;
      currentLedgerTimeOffset = 0;
      closeTimePeriodMenu();
      renderTimeTabs(); 
    }

    function renderTimeTabs() {
      const container = document.getElementById('ledgerTimeTabs');
      container.innerHTML = '';
      
      if (currentLedgerTimeMode === 'Tất cả') {
         let div = document.createElement('div');
         div.className = 'time-tab active';
         div.innerText = 'TẤT CẢ THỜI GIAN';
         container.appendChild(div);
         document.getElementById('btnBackToToday').style.display = 'none';
      } else {
         let offsets = [currentLedgerTimeOffset - 1, currentLedgerTimeOffset, currentLedgerTimeOffset + 1];
         
         offsets.forEach(off => {
            let rangeInfo = getLedgerTimeRange(currentLedgerTimeMode, off);
            let div = document.createElement('div');
            div.className = `time-tab ${off === currentLedgerTimeOffset ? 'active' : ''}`;
            div.innerText = rangeInfo.label;
            
            div.onclick = () => {
               currentLedgerTimeOffset = off; 
               renderTimeTabs(); 
            };
            container.appendChild(div);
         });
         document.getElementById('btnBackToToday').style.display = (currentLedgerTimeOffset !== 0) ? 'block' : 'none';
      }
      
      renderLedgerTransactions(); 
    }

    function backToToday() {
      currentLedgerTimeOffset = 0;
      renderTimeTabs();
    }
    // --- BIẾN ĐIỀU KHIỂN CHẾ ĐỘ XEM THEO NHÓM ---
    let ledgerGroupMode = 'time'; 

    function toggleLedgerMainGroupMode() {
      if (ledgerGroupMode === 'time') {
        setLedgerGroupMode('category-parent');
        document.getElementById('menuGroupToggle').innerText = "Xem theo thời gian";
      } else {
        setLedgerGroupMode('time');
        document.getElementById('menuGroupToggle').innerText = "Xem theo nhóm";
      }
    }

    function setLedgerGroupMode(mode) {
      ledgerGroupMode = mode;
      closeLedgerMainMenu();
      
      const toggleDiv = document.getElementById('ledgerGroupToggle');
      if (mode === 'time') {
        toggleDiv.style.display = 'none';
      } else {
        toggleDiv.style.display = 'flex';
        document.getElementById('btnGroupParent').classList.toggle('active', mode === 'category-parent');
        document.getElementById('btnGroupChild').classList.toggle('active', mode === 'category-child');
      }
      renderLedgerTransactions();
    }

    // --- BỘ NÃO VẼ GIAO DỊCH (TÍCH HỢP XEM THEO NGÀY & NHÓM) ---
    function renderLedgerTransactions() {
      const container = document.getElementById('ledgerTxContainer');
      container.innerHTML = '';
      
      let incomeTotal = 0;
      let expenseTotal = 0;
      let currentRange = getLedgerTimeRange(currentLedgerTimeMode, currentLedgerTimeOffset);
      let startTime = currentRange.start.getTime();
      let endTime = currentRange.end.getTime();
      
      let filteredData = allLedgerData.filter(tx => {
         if (!selectedLedgerWallets.includes('all') && !selectedLedgerWallets.includes(tx.wallet)) return false;
         if (currentLedgerTimeMode !== 'Tất cả') {
            let txTime = new Date(tx.date).getTime();
            if (txTime < startTime || txTime > endTime) return false;
         }
         return true;
      });

      filteredData.forEach(tx => {
         let amt = Number(tx.amount) || 0;
         let isDebt = tx.type === "Vay/Nợ" || tx.category.includes("vay") || tx.category.includes("nợ");
         let isIncome = (tx.type === "Khoản thu" || (isDebt && (tx.category.includes("Đi vay") || tx.category.includes("Thu nợ"))));
         
         if (isIncome) incomeTotal += amt;
         else expenseTotal += amt;
      });

      document.getElementById('ledgerIncomeTotal').innerText = `+¥ ${incomeTotal.toLocaleString()}`;
      document.getElementById('ledgerIncomeTotal').style.color = '#34c759';
      document.getElementById('ledgerExpenseTotal').innerText = `-¥ ${expenseTotal.toLocaleString()}`;
      document.getElementById('ledgerExpenseTotal').style.color = '#ff3b30';
      
      let grandTotal = incomeTotal - expenseTotal;
      let grandSign = grandTotal >= 0 ? "" : "-"; 
      let grandColor = grandTotal >= 0 ? "var(--text-main)" : "#ff3b30";
      
      document.getElementById('ledgerGrandTotal').innerText = `${grandSign}¥ ${Math.abs(grandTotal).toLocaleString()}`;
      document.getElementById('ledgerGrandTotal').style.color = grandColor;
      document.getElementById('ledgerWalletBalance').innerText = `${grandSign}¥ ${Math.abs(grandTotal).toLocaleString()}`; 

      if (filteredData.length === 0) {
        container.innerHTML = '<div style="text-align:center; padding: 30px; color: var(--text-muted);">Sạch sẽ! Không có giao dịch nào ở ví này.</div>';
        return;
      }

      const currencySymbols = { "VND": "₫", "JPY": "¥", "USD": "$" };

      // PHÂN LUỒNG 1: XEM THEO THỜI GIAN (NGÀY)
      if (ledgerGroupMode === 'time') {
         const grouped = {};
         const daysVN = ["Chủ Nhật", "Thứ Hai", "Thứ Ba", "Thứ Tư", "Thứ Năm", "Thứ Sáu", "Thứ Bảy"];
         
         filteredData.forEach(tx => {
           let d = new Date(tx.date);
           let dayName = daysVN[d.getDay()];
           let dd = String(d.getDate()).padStart(2, '0');
           let mm = d.getMonth() + 1;
           let yyyy = d.getFullYear();
           let dateStr = `${dd} thg ${mm} ${yyyy}, ${dayName}`;
           
           if(!grouped[dateStr]) grouped[dateStr] = [];
           grouped[dateStr].push(tx);
         });

         for (const date in grouped) {
           let dayHtml = `<div class="card" style="padding: 0 15px; margin-bottom: 15px;">
              <div style="padding: 15px 0; border-bottom: 1px solid var(--border-color); font-weight: 700; font-size: 13px; color: var(--text-muted); text-transform: uppercase; letter-spacing: 1px;">
                ${date}
              </div>`;
              
           grouped[date].forEach(tx => {
              let isDebt = tx.type === "Vay/Nợ" || tx.category.includes("vay") || tx.category.includes("nợ");
              let isIncome = (tx.type === "Khoản thu" || (isDebt && (tx.category.includes("Đi vay") || tx.category.includes("Thu nợ"))));
              let sign = isIncome ? "+" : "-";
              let colorClass = isIncome ? "color-income" : "color-expense";
              let catIcon = getAiCatIcon(tx.category, tx.type);
              let sym = currencySymbols[tx.currency] || tx.currency;
              
              dayHtml += `
                <div style="display: flex; justify-content: space-between; align-items: center; padding: 15px 0; border-bottom: 1px dashed var(--border-color);">
                   <div style="display: flex; align-items: center; gap: 15px; flex: 1; min-width: 0;">
                      <div class="color-icon" style="background: var(--bg-main); border: 1px solid var(--border-color); width: 38px; height: 38px; font-size: 18px; flex-shrink: 0;">${catIcon}</div>
                      <div style="flex: 1; min-width: 0; padding-right: 10px;">
                        <div style="font-weight: 600; font-size: 15px; color: var(--text-main); white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">${tx.category.split(' - ').pop()}</div>
                        <div style="font-size: 12px; color: var(--text-muted); margin-top: 4px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">${tx.wallet} ${tx.note ? '• '+tx.note : ''} ${tx.person ? '• 👤 '+tx.person : ''}</div>
                      </div>
                   </div>
                   <div style="font-weight: 700; font-size: 16px; white-space: nowrap; flex-shrink: 0; text-align: right;" class="${colorClass}">
                      ${sign}${sym} ${parseInt(tx.amount).toLocaleString()}
                   </div>
                </div>
              `;
           });
           dayHtml += `</div>`;
           container.insertAdjacentHTML('beforeend', dayHtml);
         }
      } 
      // PHÂN LUỒNG 2: XEM THEO NHÓM (CHA / CON)
      else {
         const grouped = {};
         
         filteredData.forEach(tx => {
            let parts = tx.category.split(' - ');
            let parentCat = parts[0];
            let childCat = parts.length > 1 ? parts[1] : parts[0];
            
            // Lọc theo Cha hay Con tùy vào nút gạt
            let groupKey = ledgerGroupMode === 'category-parent' ? parentCat : tx.category;
            let displayCatName = ledgerGroupMode === 'category-parent' ? parentCat : childCat;
            
            if (!grouped[groupKey]) {
               grouped[groupKey] = {
                  name: displayCatName,
                  totalIncome: 0,
                  totalExpense: 0,
                  txs: [],
                  type: tx.type, 
                  fullCatName: tx.category 
               };
            }
            
            grouped[groupKey].txs.push(tx);
            let amt = Number(tx.amount) || 0;
            let isDebt = tx.type === "Vay/Nợ" || tx.category.includes("vay") || tx.category.includes("nợ");
            let isIncome = (tx.type === "Khoản thu" || (isDebt && (tx.category.includes("Đi vay") || tx.category.includes("Thu nợ"))));
            
            if (isIncome) grouped[groupKey].totalIncome += amt;
            else grouped[groupKey].totalExpense += amt;
         });
         
         // Biến thành mảng, tính tổng tiền Net, xếp thằng tiêu nhiều nhất lên đầu
         let groupArr = Object.values(grouped);
         groupArr.sort((a, b) => {
            let aTotal = Math.abs(a.totalIncome - a.totalExpense);
            let bTotal = Math.abs(b.totalIncome - b.totalExpense);
            return bTotal - aTotal; 
         });
         
         groupArr.forEach(group => {
            let net = group.totalIncome - group.totalExpense;
            let isNetIncome = net >= 0;
            if (group.totalIncome === 0 && group.totalExpense > 0) isNetIncome = false; 
            
            let sign = isNetIncome ? "+" : "-";
            let colorClass = isNetIncome ? "color-income" : "color-expense";
            let catIcon = getAiCatIcon(group.fullCatName, group.type);
            let sym = group.txs.length > 0 ? (currencySymbols[group.txs[0].currency] || group.txs[0].currency) : "₫";

            let groupHtml = `<div class="card" style="padding: 0 15px; margin-bottom: 15px;">
               <div style="display: flex; justify-content: space-between; align-items: center; padding: 15px 0; border-bottom: 1px solid var(--border-color);">
                  <div style="display: flex; align-items: center; gap: 15px; flex: 1; min-width: 0;">
                     <div class="color-icon" style="background: var(--form-bg); border: 1px solid var(--border-color); width: 38px; height: 38px; font-size: 18px; flex-shrink: 0;">${catIcon}</div>
                     <div style="flex: 1; min-width: 0; padding-right: 10px;">
                       <div style="font-weight: 700; font-size: 15px; color: var(--text-main); white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">${group.name}</div>
                       <div style="font-size: 12px; color: var(--text-muted); margin-top: 4px;">${group.txs.length} giao dịch</div>
                     </div>
                  </div>
                  <div style="font-weight: 700; font-size: 16px; white-space: nowrap; flex-shrink: 0; text-align: right;" class="${colorClass}">
                     ${sign}${sym} ${Math.abs(net).toLocaleString()}
                  </div>
               </div>`;
               
            // Dòng chi tiết Giao dịch con bên trong
            group.txs.forEach(tx => {
               let txIsDebt = tx.type === "Vay/Nợ" || tx.category.includes("vay") || tx.category.includes("nợ");
               let txIsIncome = (tx.type === "Khoản thu" || (txIsDebt && (tx.category.includes("Đi vay") || tx.category.includes("Thu nợ"))));
               let txSign = txIsIncome ? "+" : "-";
               let txColorClass = txIsIncome ? "color-income" : "color-expense";
               let txSym = currencySymbols[tx.currency] || tx.currency;
               
               let d = new Date(tx.date);
               let dateStr = `${String(d.getDate()).padStart(2, '0')}/${String(d.getMonth() + 1).padStart(2, '0')}`;
               
               // Bí kíp: Đang xem nhóm Cha thì hiện tên nhóm con. Đang xem nhóm Con thì hiện Ghi chú.
               let subTitle = ledgerGroupMode === 'category-parent' ? tx.category.split(' - ').pop() : (tx.note || "Không có ghi chú");

               groupHtml += `
                 <div style="display: flex; justify-content: space-between; align-items: center; padding: 12px 0; border-bottom: 1px dashed var(--border-color); margin-left: 53px;">
                    <div style="flex: 1; min-width: 0; padding-right: 10px;">
                       <div style="font-weight: 500; font-size: 14px; color: var(--text-main); white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">${subTitle}</div>
                       <div style="font-size: 11px; color: var(--text-muted); margin-top: 2px;">${dateStr} • ${tx.wallet} ${tx.person ? '• 👤 '+tx.person : ''}</div>
                    </div>
                    <div style="font-weight: 600; font-size: 14px; white-space: nowrap; flex-shrink: 0; text-align: right;" class="${txColorClass}">
                       ${txSign}${txSym} ${parseInt(tx.amount).toLocaleString()}
                    </div>
                 </div>
               `;
            });
            groupHtml += `</div>`;
            container.insertAdjacentHTML('beforeend', groupHtml);
         });
      }

      document.querySelectorAll('.card > div:last-child').forEach(el => {
         el.style.borderBottom = 'none';
      });
    }
  </script>
</body>
</html>
